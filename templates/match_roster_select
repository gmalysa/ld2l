<div class="ld2l-lobby-side" id="{side}">
  <span class="ld2l-lobby-team {side}">{team.name}</span>
  {#team.players}
    <div class="ld2l-lobby-player pure-control-group">
	  {@eq key=captainSide value=captainCheck}
        <input type="checkbox"
			   {?inGameRoster} checked {/inGameRoster}
		       name="check-{steamid}" />
      {:else}
	    {?inGameRoster}<span class="fa fa-check"></span>{/inGameRoster}
	  {/eq}
  	  {>roster_card /}
    </div>
  {/team.players}
  <div class="ld2l-lobby-player pure-control-group">
	{@eq key=captainSide value=captainCheck}
      <input type="checkbox" name="use-standin" />{~n}
	  <input type="text"
	         name="standin"
			 id="standin"
			 value="{team.standin}"
	  	     placeholder="Standin"
	         class="ld2l-name-autocomplete" />
	{/eq}
  </div>
</div>
